import { createContext, useEffect } from "react";
import { tokenHeader } from "../constant/tokenHeader";
import axios from "axios";

const UserContext = createContext();
export default UserContext;

export const GetUser = (dispatchUser) => {
  useEffect(() => {
  
  }, []);
};
